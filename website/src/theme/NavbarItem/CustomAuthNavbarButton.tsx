import React from 'react';
import AuthNavbarButton from '@site/src/components/AuthNavbarButton';

const CustomAuthNavbarButton = () => {
  return <AuthNavbarButton />;
};

export default CustomAuthNavbarButton;